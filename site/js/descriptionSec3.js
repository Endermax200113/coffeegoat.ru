//Заполнение описании меню

function fillInMenuSec3() {
	preparate();

	function preparate() {
		// $(".sec3>.wrap>.blocks>.line").each(function(i1, el1) {
		// 	$(".sec3>.wrap>.blocks>.line").eq(i1).find(".block").each(function(i2, el2) {
		// 		for (let i = 0; i < 5; i++) {
		// 			kv.menuSec3.addItem(i1, i2, i, "Гляссе " + i, 130, "/img/sec3-desc.png", ""
		// 				+ "Раф-кофе — популярный в России и странах бывшего "
		// 				+ "СССР кофейный напиток появившийся в конце 1990-х "
		// 				+ "годов. Готовится путём добавления нагретых паром "
		// 				+ "сливок с небольшим количеством пены в одиночную "
		// 				+ "порцию эспрессо. Основное отличие от латте — в "
		// 				+ "использовании ванильного сахара и сливок вместо "
		// 				+ "молока.")
		// 		}

		// 		// $(".sec3>.wrap>.blocks>.line").eq(i1).find(".block").eq(i2).find(".list>ul").each(function(i3, el3) {
		// 		// 	$(".sec3>.wrap>.blocks>.line").eq(i1).find(".block").eq(i2).find(".list>ul>li").eq(i3).on("mousemove", function(e) {
		// 		// 		$(".sec3>.wrap>.blocks>.line").eq(i1).find(".block").eq(i2).find(".list>ul>li").eq(i3).find(".desc").addClass("show");
		// 		// 	});

		// 		// 	$(".sec3>.wrap>.blocks>.line").eq(i1).find(".block").eq(i2).find(".list>ul>li").eq(i3).on("mouseleave", function(e) {
		// 		// 		$(".sec3>.wrap>.blocks>.line").eq(i1).find(".block").eq(i2).find(".list>ul>li").eq(i3).find(".desc").removeClass("show");
		// 		// 	});
		// 		// });
		// 	});
		// });
	}
}